# hello-world

The smallest end-to-end capability: start with a fixed payload, enrich it with
an HTTP call to a public API, format a greeting, and log it.

Shows a source, `enrich(http(...))`, `transform`, and a `log` destination.
